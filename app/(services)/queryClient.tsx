import { QueryClient, useQueryClient } from "@tanstack/react-query";

const queryClient = new QueryClient();
// console.log("Jumlah request yang sedang fetching:", isFetching);

export default queryClient;
